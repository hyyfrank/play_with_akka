# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Restful Service API For Singel CRUD
GET     /rest/all                   com.autodesk.www.controllers.PersonController.getPersons
GET     /rest/:name                 com.autodesk.www.controllers.PersonController.getPersonByName(name:String)
# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               com.autodesk.www.controllers.Assets.versioned(path="/public", file: Asset)
